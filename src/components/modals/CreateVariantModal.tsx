import React from 'react';

// This component is intentionally left empty as per user request to remove this feature.
const CreateVariantModal: React.FC = () => {
  return null;
};

export default CreateVariantModal;