import React from 'react';

// This component is intentionally left empty. Its functionality for setting per-order
// prices has been removed in favor of a centralized master price system. This aligns
// with the new logic where order items dynamically reflect master price changes.
const SetUnitPriceModal: React.FC = () => {
  return null;
};

export default SetUnitPriceModal;